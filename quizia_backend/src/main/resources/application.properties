# SQLite datasource
spring.datasource.url=jdbc:sqlite:quizia.db
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.initialization-mode=never

# Server port (optional)
server.port=8081
spring.application.name=quizia_backend
